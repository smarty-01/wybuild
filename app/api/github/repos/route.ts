import {NextResponse} from "next/server";import {cookies} from "next/headers";
export async function GET(){const token=cookies().get("github_access_token")?.value;if(!token)return NextResponse.json({error:"Connect GitHub first"},{status:401});const r=await fetch("https://api.github.com/user/repos?sort=updated&per_page=100",{headers:{Authorization:`Bearer ${token}`,Accept:"application/vnd.github+json"},cache:"no-store"});if(!r.ok)return NextResponse.json({error:"GitHub API error"},{status:r.status});const x=await r.json();return NextResponse.json(x.map((a:any)=>({id:a.id,name:a.name,full_name:a.full_name,private:a.private,default_branch:a.default_branch,clone_url:a.clone_url})))}