import {NextResponse} from "next/server";
export async function GET(req:Request){const u=new URL(req.url);if(!process.env.GITHUB_CLIENT_ID)return NextResponse.json({error:"GITHUB_CLIENT_ID missing"},{status:503});const state=crypto.randomUUID(),redirect=process.env.GITHUB_REDIRECT_URI||`${u.origin}/api/auth/github/callback`;const r=NextResponse.redirect(`https://github.com/login/oauth/authorize?client_id=${encodeURIComponent(process.env.GITHUB_CLIENT_ID)}&redirect_uri=${encodeURIComponent(redirect)}&scope=repo&state=${state}`);r.cookies.set("oauth_state",state,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",maxAge:600,path:"/"});return r}